version: 2.1
orbs:
  cypress: cypress-io/cypress@1
  build:
    jobs:
      - cypress/run:
          yarn: true
          no-workspace: true
          start: npm start
          working_directory: client